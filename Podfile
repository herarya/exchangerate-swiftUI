 if let items = self.currencies, items.count > 0 {
                                CurrencyScrollView(items: items, position: findIndexCurrencies(code: baseCode), onScrollChange: {
                                    currency in
                                    baseCode = currency.code
//                                    self.onChange()
                                })
                            }